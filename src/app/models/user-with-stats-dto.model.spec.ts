import { UserWithStatsDto } from './user-with-stats-dtomodel';

describe('UserWithStatsDto', () => {
  it('should create an instance', () => {
    expect(new UserWithStatsDto()).toBeTruthy();
  });
});
