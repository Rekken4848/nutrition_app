import { RecipeIngredient } from './recipe-ingredientmodel';

describe('RecipeIngredient', () => {
  it('should create an instance', () => {
    expect(new RecipeIngredient()).toBeTruthy();
  });
});
