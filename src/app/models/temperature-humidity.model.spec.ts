import { TemperatureHumidity } from './temperature-humiditymodel';

describe('TemperatureHumidity', () => {
  it('should create an instance', () => {
    expect(new TemperatureHumidity()).toBeTruthy();
  });
});
