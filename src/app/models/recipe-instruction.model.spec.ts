import { RecipeInstruction } from './recipe-instructionmodel';

describe('RecipeInstruction', () => {
  it('should create an instance', () => {
    expect(new RecipeInstruction()).toBeTruthy();
  });
});
